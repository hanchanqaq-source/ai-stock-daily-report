# 股票基金双中心 Build E5：持仓变更历史

本机 SQLite 会按用户记录快速持仓的新增、编辑、删除到回收站和恢复操作。股票和基金保持独立标识；页面只显示当前用户的最近 50 条变更，便于核对“哪一笔、何时、做了什么操作”。

历史记录只保存当次持仓的代码、名称和必要金额/数量字段，不保存截图、密钥、设置、日志、真实账户或交易流水。接口继续只允许 `127.0.0.1`。

历史不反向修改持仓，也不提供自动撤销；误删仍通过既有回收站“恢复最近删除”处理。
