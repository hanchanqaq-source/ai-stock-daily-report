# 股票基金双中心 Build E6：持仓历史前后对比

快速持仓被编辑时，变更历史会保存编辑前与编辑后的必要持仓字段，并在当前股票或基金中心显示对比。新增、删除、恢复仍保留单次快照；旧历史记录继续兼容显示。

为兼容已有本机 SQLite 数据，本阶段不新增数据库列：编辑对比以既有历史快照字段的受控 JSON 结构保存。历史不保存截图、密钥、设置、日志、真实账户或交易流水，接口继续只允许 `127.0.0.1`。
